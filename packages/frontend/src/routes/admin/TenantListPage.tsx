import { Typography } from 'antd';

export function TenantListPage() {
  return (
    <div>
      <Typography.Title level={3}>대학 관리</Typography.Title>
      <Typography.Text type="secondary">대학 목록 — Phase 4 이후 구현</Typography.Text>
    </div>
  );
}
