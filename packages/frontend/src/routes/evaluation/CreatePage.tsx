import { Typography } from 'antd';

export function CreatePage() {
  return (
    <div>
      <Typography.Title level={3}>평가 생성</Typography.Title>
      <Typography.Text type="secondary">유형 선택 + 기본 정보 — Phase 4 이후 구현</Typography.Text>
    </div>
  );
}
